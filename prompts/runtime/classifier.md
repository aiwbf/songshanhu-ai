# Runtime Classifier Prompt Skeleton

任务：

- 从用户消息和已有 `CaseProfile` 中识别咨询意图
- 补充事实标签
- 对 A1/A2/A3/B1/B2/B3/C 做分类尝试
- 当信息不足时明确列出缺失字段

分类约束：

- 只输出“当前可判断到的类别”或“暂无法判断”
- 不输出没有证据支撑的资格承诺
- 如果命中特殊政策或流程标签，必须显式返回

输出应优先给结构化字段，而不是自然语言长文。
